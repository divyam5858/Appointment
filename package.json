{
  "name": "appointment",
  "version": "0.1.0",

  "homepage": "https://divyam5858.github.io/Appointment",

  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",

    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },

  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}